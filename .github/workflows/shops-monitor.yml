env:
  MODE: ${{ steps.mode.outputs.MODE }}
  DOMAINS_FILE: domains.txt
  STATE_FILE: state.json
  CHECK_PATH: /
  TIMEOUT_MS: "8000"
  FAIL_THRESHOLD: "3"
  CONCURRENCY: "30"
  BATCH_SIZE: "250"
  MAX_REPORT_ITEMS: "120"
  TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
  TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
